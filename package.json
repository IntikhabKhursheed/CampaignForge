{
  "name": "campaignforge-root",
  "private": true,
  "version": "1.0.0",
  "license": "MIT",
  "description": "Root of CampaignForge monorepo (no workspaces). Run commands from client/ or server/",
  "scripts": {
    "postinstall": "echo 'Install dependencies inside client/ and server/ separately'"
  }
}
